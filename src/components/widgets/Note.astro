---
import { Icon } from 'astro-icon/components';

export interface Props {
  icon?: string;
  title?: string;
  description?: string;
}

const {
  icon = 'tabler:info-square',
  title = await Astro.slots.render('title'),
  description = await Astro.slots.render('description'),
} = Astro.props;
---

<section class="not-prose py-4">
  <div class="max-w-5xl mx-auto px-4 sm:px-6">
    <div
      class="rounded-lg border border-page-border bg-surface px-5 py-4 text-md text-center font-medium shadow-[0_4px_30px_rgba(17,24,39,0.08)] backdrop-blur dark:shadow-none md:px-6"
    >
      <Icon name={icon} class="w-5 h-5 inline-block align-text-bottom font-bold" />
      <span class="font-bold" set:html={title} />
      <Fragment set:html={description} />
    </div>
  </div>
</section>
