---
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';
import Timeline from '~/components/ui/Timeline.astro';
import Image from '~/components/common/Image.astro';
import type { Steps as Props } from '~/types';

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  tagline = await Astro.slots.render('tagline'),
  items = [],
  image = await Astro.slots.render('image'),
  isReversed = false,

  id,
  isDark = false,
  classes = {},
  bg = await Astro.slots.render('bg'),
} = Astro.props;

const hasHeader = title || subtitle || tagline;
---

<WidgetWrapper id={id} isDark={isDark} containerClass={`max-w-5xl mx-auto ${classes?.container ?? ''}`} bg={bg}>
  {
    hasHeader && (
      <div class="mb-8 rounded-lg border border-page-border bg-surface px-5 py-4 shadow-[0_4px_30px_rgba(17,24,39,0.08)] backdrop-blur dark:shadow-none md:px-6">
        {tagline && <p class="mb-1 text-xs font-semibold uppercase tracking-wide text-primary" set:html={tagline} />}
        {title && <h2 class="text-xl font-bold tracking-tight text-heading md:text-2xl" set:html={title} />}
        {subtitle && <p class="mt-1 max-w-3xl text-sm leading-6 text-muted md:text-base" set:html={subtitle} />}
      </div>
    )
  }

  <div class={image ? 'grid gap-10 md:grid-cols-2 md:gap-16' : 'w-full'}>
    <div class:list={['self-center', { 'md:order-2': image && isReversed }, { 'w-full': !image }]}>
      <Timeline items={items} classes={classes?.items as Record<string, never>} />
    </div>
    {
      image && (
        <div class:list={['self-center', { 'md:order-1': isReversed }]}>
          {typeof image === 'string' ? (
            <Fragment set:html={image} />
          ) : (
            <Image
              class="mx-auto w-full rounded-lg bg-surface-raised shadow-lg shadow-slate-900/10 dark:shadow-black/30"
              widths={[400, 768]}
              sizes="(max-width: 768px) 100vw, 432px"
              width={500}
              height={500}
              layout="responsive"
              src={image?.src}
              alt={image?.alt || ''}
            />
          )}
        </div>
      )
    }
  </div>
</WidgetWrapper>
