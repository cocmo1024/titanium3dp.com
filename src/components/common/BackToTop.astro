---
import { Icon } from 'astro-icon/components';
---

<button
  type="button"
  class="fixed bottom-5 right-4 z-30 inline-flex h-11 w-11 translate-y-3 items-center justify-center rounded-full border border-page-border bg-surface text-page opacity-0 shadow-lg shadow-slate-900/10 backdrop-blur transition duration-200 ease-out hover:border-accent hover:text-accent focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2 focus:ring-offset-[#f7f8fa] disabled:pointer-events-none dark:shadow-black/30 dark:focus:ring-accent dark:focus:ring-offset-[#050814] md:bottom-6 md:right-6 md:h-12 md:w-12"
  aria-label="Back to top"
  data-aw-back-to-top
  hidden
>
  <Icon name="tabler:arrow-up" class="h-5 w-5" />
</button>

<script is:inline>
  (() => {
    const button = document.querySelector('[data-aw-back-to-top]');
    if (!button) return;

    const visibleClass = ['opacity-100', 'translate-y-0'];
    const hiddenClass = ['opacity-0', 'translate-y-3'];
    let ticking = false;

    function setVisible(isVisible) {
      button.hidden = false;
      button.tabIndex = isVisible ? 0 : -1;
      button.setAttribute('aria-hidden', String(!isVisible));
      button.classList.toggle('pointer-events-none', !isVisible);
      button.classList.toggle('invisible', !isVisible);
      visibleClass.forEach((className) => button.classList.toggle(className, isVisible));
      hiddenClass.forEach((className) => button.classList.toggle(className, !isVisible));
    }

    function updateVisibility() {
      setVisible(window.scrollY > 520);
      ticking = false;
    }

    button.addEventListener('click', () => {
      const behavior = window.matchMedia('(prefers-reduced-motion: reduce)').matches ? 'auto' : 'smooth';
      window.scrollTo({ top: 0, behavior });
    });

    window.addEventListener(
      'scroll',
      () => {
        if (ticking) return;
        window.requestAnimationFrame(updateVisibility);
        ticking = true;
      },
      { passive: true }
    );

    updateVisibility();
  })();
</script>
